{
    "buildCommand": "npm run build",
    "outputDirectory": "public",
    "framework": null,
    "rewrites": [
        {
            "source": "/api/:match*",
            "destination": "/api/:match*"
        }
    ]
}